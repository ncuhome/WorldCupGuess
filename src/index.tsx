/* @refresh reload */
import { render } from "solid-js/web";

import "./index.css";
import App from "./App";

// render(() => <App />, document.getElementById("root") as HTMLElement);
render(() => <div>维护中...</div>, document.getElementById("root") as HTMLElement);
